type: target-group
name: Internal Test Team
conditions:
  - operator: in-array
    property: plan
    operand:
      - Tester
  - operator: regex
    property: user_email
    operand: '*@cloudbees.com'
